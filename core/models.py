from django.db import models
from django.contrib.auth.models import User


class login(models.Model):
    def__init__(self, username):
        self.username = nombre

class login(models.Model):
    def__init__ (self, passsword):
        self.password = contraseña

class cpassword(models.Model):
    def__init__(self.password):
        self.passsword = contraseña

class cpassword(models.Model):
    def__init__(self.newpassword):
        self.newpassword = contraseña nueva

class cpassword(models.Model):
    def__init__(self.newpassword):
        self.comfirmpassword = comfirmar contraseña

class menu(models.Model):
    def__init__(self.menu):
        self.menu = menu


# Create your models here.